import { styled } from "@stitches/react";
import * as DropdownMenuPrimitive from "@radix-ui/react-dropdown-menu";

const DropdownArrow = styled(DropdownMenuPrimitive.Arrow, {
  fill: "white",
});

export default DropdownArrow;
